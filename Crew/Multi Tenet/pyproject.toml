[project]
name = "tenantdesk-backend"
version = "0.1.0"
description = "Multi-tenant AI support platform — backend API"
requires-python = ">=3.11"

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
addopts = "-q"
